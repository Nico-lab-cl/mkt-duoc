import * as lucide from 'lucide-react';
console.log('User:', lucide.User ? 'exists' : 'missing');
console.log('AlertCircle:', lucide.AlertCircle ? 'exists' : 'missing');
console.log('Users:', lucide.Users ? 'exists' : 'missing');
